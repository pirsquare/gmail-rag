# Core LLM library
openai>=1.7.0

# Vector Database
chromadb>=0.4.22

# Local Embeddings (sentence-transformers directly)
sentence-transformers>=2.3.0

# LangChain (minimal - just for Document schema and text splitting)
langchain>=0.1.0
langchain-text-splitters>=0.0.1

# Gmail Integration
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0

# UI
streamlit>=1.30.0

# Utilities
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
tqdm>=4.66.0
# Compatibility
pydantic<2.0.0           # chromadb currently requires pydantic v1
# ML runtime for sentence-transformers
torch>=2.1.0